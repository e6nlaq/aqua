
#include <bits/stdc++.h>
#include "./lib/math.h"
using namespace std;
using ll = long long;

#define co(x) cout << (x) << "\n"
#define rep(i, n) for (ll i = 0; i < (n); i++)

int main(int argc, char const *argv[])
{
	if (NULL)
	{
		co("Yes");
	}
	else
	{
		co("No");
	}

	return 0;
}
